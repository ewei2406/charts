apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "ftb-stoneblock.fullname" . }}-world-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.pvc.storage }}
